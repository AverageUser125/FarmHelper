package com.jelly.farmhelper.misc;

public interface TitleRendering {
    void farmhelper_mod$setRenderTitle(String title, int stayTicks, int yOffset, float scale, RenderColor color);

    boolean farmhelper_mod$isRenderingTitle();
}